version: 2
updates:
  # Keep GitHub Actions versions current (one grouped PR per month).
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    groups:
      actions:
        patterns: ["*"]
